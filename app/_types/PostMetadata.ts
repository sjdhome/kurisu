type PostMetadata = {
  id: string;
  title: string;
  created: string;
  edited: string;
  author: string;
  tags: string[];
  description: string;
  showComment: boolean;
  visible: boolean;
  pinToTop: boolean;
};

export type { PostMetadata };
